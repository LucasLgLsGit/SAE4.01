{% extends './_template/base.html.twig' %}

{% block body %}
<div class="container mt-4">
	<h1 class="mb-4 text-center">Boutique</h1>
	<div class="row">
		{% for produit in produits %}
		<div class="col-12 col-sm-6 col-md-4 col-lg-3 mb-4">
			<a href="/shop/detail.php?id={{ produit.id_produit }}" class="text-decoration-none text-dark">
				<div class="card h-100">
					<div class="card-img-top d-flex justify-content-center align-items-center" style="height: 200px; background-color: #f8f9fa;">
						<img src="#" alt="image" class="img-fluid" style="max-height: 100%; max-width: 100%;">
					</div>
					<div class="card-body text-center">
						<h5 class="card-title">{{ produit.titre_produit }}</h5>
						<p class="card-text text-muted">{{ produit.date_produit|date('Y') }}</p>
						<p class="card-text font-weight-bold">{{ produit.prix }} €</p>
					</div>
				</div>
			</a>
		</div>
		{% endfor %}
	</div>
</div>
{% endblock %}